package model;

import it.unical.mat.embasp.languages.Id;

@Id("inFinalPath")
public class InFinalPath extends Casella {

	public InFinalPath() {
		super();
	}

	public InFinalPath(int row, int col) {
		super(row, col);
	}
}
